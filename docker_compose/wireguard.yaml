---
services:
  wireguard: #51820
    image: lscr.io/linuxserver/wireguard
    container_name: wireguard
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Los_Angeles
      - SERVERURL=wireguard.yourdomain.com
      - SERVERPORT=51820
      - PEERS=laptop,tablet,phon
      - PEERDNS=auto
      - INTERNAL_SUBNET=10.0.0.2
      - ALLOWEDIPS=0.0.0.0/0
    volumes:
      - /homelab/docker/wireguard/config:/config
      - /homelab/docker/wireguard/lib/modules:/lib/modules
    ports:
      - 51820:51820/udp
    restart: unless-stopped
